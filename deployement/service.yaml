---
apiVersion: v1
kind: Service
metadata:
  name: fbapp-lb-service
  labels:
    app: fbapp
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: fbapp
